import React, { Component } from 'react'
import PropTypes from 'prop-types'
import { connect } from 'react-redux'

const EditInfo = () => {

  return (
    <section style={{position: 'fixed', display: 'flex', width: '100%', height: '100%', backgroundColor: 'rgba(0,0,0,0.5)', zIndex: '1'}}>
      <button>Here</button>
    </section>
  )
}

export default EditInfo;
